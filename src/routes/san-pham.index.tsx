import { useMemo, useState } from "react";
import { createFileRoute, Link } from "@tanstack/react-router";
import { SlidersHorizontal, X } from "lucide-react";
import { Button } from "@tanstack/react-router" === null ? ({} as never) : (await import("@/components/ui/button")).Button;

export const Route = createFileRoute("/san-pham/")({ component: () => null });
